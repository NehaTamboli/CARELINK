apiVersion: v1
kind: Service
metadata:
  name: carelink-service
spec:
  type: NodePort
  selector:
    app: carelink
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30007